---
phase: 04-browse-discovery-and-profiles
plan: 03
type: execute
wave: 1
depends_on: [04-02]
files_modified:
  - client/src/pages/Explore.jsx
  - client/src/components/PostCard.jsx
  - client/src/components/PostListItem.jsx
  - client/src/pages/ViewPost.jsx
  - client/src/App.jsx
  - client/src/context/ProfilePanelContext.jsx
  - client/src/components/ProfilePanel.jsx
autonomous: true
requirements: [DISC-03, PROF-02]
gap_closure: true

must_haves:
  truths:
    - "Visitor clicks an artist name on a post card and sees all posts about that artist on /artist/:name"
    - "Explore page shows artists section with correct names and images from the API"
    - "Clicking author name on any post navigates to /u/{username} profile page (not a slide-out panel)"
    - "No ProfilePanel slide-out or backdrop appears anywhere in the app"
  artifacts:
    - path: "client/src/pages/Explore.jsx"
      provides: "Artists section using correct API field names (name, image)"
      contains: "artist.name"
    - path: "client/src/components/PostCard.jsx"
      provides: "Author name as Link to /u/{username}"
      contains: "to={`/u/"
    - path: "client/src/components/PostListItem.jsx"
      provides: "Author name as Link to /u/{username}"
      contains: "to={`/u/"
    - path: "client/src/pages/ViewPost.jsx"
      provides: "Author name as Link to /u/{username}"
      contains: "to={`/u/"
    - path: "client/src/App.jsx"
      provides: "No ProfilePanelProvider or ProfilePanel references"
  key_links:
    - from: "client/src/pages/Explore.jsx"
      to: "/api/browse/explore"
      via: "Correct field mapping: artist.name, artist.image"
      pattern: "artist\\.name"
    - from: "client/src/components/PostCard.jsx"
      to: "client/src/pages/Profile.jsx"
      via: "Link to /u/{username} on author display name"
      pattern: "to=\\{`/u/"
---

<objective>
Fix two UAT gaps from Phase 4 testing: (1) artist functionality broken by field name mismatch in Explore.jsx, and (2) author clicks should navigate to full profile page instead of opening a slide-out panel.

Purpose: Close the 3 failing UAT tests (Tests 2, 3, 5) so browse/discovery/profiles work as the user expects.
Output: Fixed Explore.jsx field references, author clicks converted from openProfile() to Link navigation, ProfilePanel and ProfilePanelContext deleted, App.jsx cleaned up.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-browse-discovery-and-profiles/04-01-SUMMARY.md
@.planning/phases/04-browse-discovery-and-profiles/04-UAT.md

<interfaces>
<!-- API response from GET /api/browse/explore (from server/routes/browse.js) -->
```javascript
// The explore endpoint maps DB columns to these field names:
{
  tags: [{ tag, latestPostAt }],
  artists: [{ name, image, latestPostAt }],   // NOT artist_name, artist_image
  contributors: [{ username, displayName, latestPostAt }]
}
```

<!-- Profile page route -->
```javascript
// Route in App.jsx: <Route path="/u/:username" element={<Profile />} />
// Link format: <Link to={`/u/${username}`}>
```

<!-- Auth context (used by PostCard/ViewPost for edit link, not for profile navigation) -->
```javascript
// useAuth() returns { user, login, logout, loading }
// user = { id, email, displayName, username, role } or null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Explore.jsx artist field name mismatch</name>
  <files>client/src/pages/Explore.jsx</files>
  <action>
Fix the field name mismatch in the Artists section of Explore.jsx. The API (GET /api/browse/explore) returns artists with fields `name` and `image`, but the component uses `artist_name` and `artist_image` (the raw DB column names).

**Changes in the artists section (lines 62-88):**
- Change `artist.artist_name` to `artist.name` (4 occurrences: key prop, encodeURIComponent link, img alt, span text)
- Change `artist.artist_image` to `artist.image` (2 occurrences: ternary condition and img src)

The contributors section already uses the correct API field names (`contributor.username`, `contributor.display_name`). Do NOT change anything else.
  </action>
  <verify>
    <automated>grep -c "artist_name\|artist_image" client/src/pages/Explore.jsx | xargs -I{} test {} -eq 0 && echo "No raw DB column names remain" && grep "artist.name" client/src/pages/Explore.jsx && echo "Correct field names in use"</automated>
  </verify>
  <done>Explore.jsx artists section uses artist.name and artist.image matching the API response shape. Artists render correctly with names and images on /explore.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ProfilePanel slide-out with full-page author navigation</name>
  <files>client/src/components/PostCard.jsx, client/src/components/PostListItem.jsx, client/src/pages/ViewPost.jsx, client/src/App.jsx, client/src/context/ProfilePanelContext.jsx, client/src/components/ProfilePanel.jsx</files>
  <action>
The user explicitly requested that clicking an author name should navigate to the full profile page at /u/{username}, not open a slide-out panel. Remove the ProfilePanel system entirely and convert all author name clicks to Link navigation.

**1. Update client/src/components/PostCard.jsx:**
- Remove the import of `useProfilePanel` from `../context/ProfilePanelContext`
- Remove the `const { openProfile } = useProfilePanel();` line
- Replace the author `<button onClick={() => openProfile(post.author.username)} ...>` with a `<Link to={`/u/${post.author.username}`} className="hover:text-gray-600 transition">{post.author.displayName}</Link>`
- Ensure `Link` is already imported from react-router-dom (it is)

**2. Update client/src/components/PostListItem.jsx:**
- Remove the import of `useProfilePanel` from `../context/ProfilePanelContext`
- Remove the `const { openProfile } = useProfilePanel();` line
- Replace the author `<button onClick={() => openProfile(post.author.username)} ...>` with a `<Link to={`/u/${post.author.username}`} className="hover:text-gray-600 transition">{post.author.displayName}</Link>`
- Ensure `Link` is imported from react-router-dom (it already is)

**3. Update client/src/pages/ViewPost.jsx:**
- Remove the import of `useProfilePanel` from `../context/ProfilePanelContext`
- Remove the `const { openProfile } = useProfilePanel();` line
- Replace the author `<button onClick={() => openProfile(post.author?.username)} ...>` with a `<Link to={`/u/${post.author?.username}`} className="hover:text-gray-600 transition">{post.author?.displayName || 'Unknown'}</Link>`
- `Link` is already imported from react-router-dom

**4. Update client/src/App.jsx:**
- Remove the import of `ProfilePanelProvider` from `./context/ProfilePanelContext`
- Remove the import of `ProfilePanel` from `./components/ProfilePanel`
- Remove the `<ProfilePanelProvider>` wrapper (lines 28 and 84) -- just delete the opening and closing tags, keep children intact
- Remove the `<ProfilePanel />` self-closing component (line 30)

**5. Delete client/src/context/ProfilePanelContext.jsx** — no longer used anywhere.

**6. Delete client/src/components/ProfilePanel.jsx** — no longer used anywhere.

After all changes, no file in the project should import from ProfilePanelContext or reference ProfilePanel.
  </action>
  <verify>
    <automated>grep -r "ProfilePanel\|openProfile\|useProfilePanel" client/src/ 2>/dev/null | grep -v node_modules; test $? -eq 1 && echo "No ProfilePanel references remain" && test ! -f client/src/context/ProfilePanelContext.jsx && echo "Context file deleted" && test ! -f client/src/components/ProfilePanel.jsx && echo "Panel file deleted" && grep "to=\`/u/" client/src/components/PostCard.jsx && echo "PostCard has /u/ link"</automated>
  </verify>
  <done>
    - Author name clicks navigate to /u/{username} profile page via Link on PostCard, PostListItem, and ViewPost
    - ProfilePanelContext.jsx and ProfilePanel.jsx are deleted
    - App.jsx no longer wraps with ProfilePanelProvider or renders ProfilePanel
    - No references to ProfilePanel, openProfile, or useProfilePanel remain in the codebase
  </done>
</task>

</tasks>

<verification>
1. `grep -r "artist_name\|artist_image" client/src/pages/Explore.jsx` returns nothing (field mismatch fixed)
2. `grep -r "ProfilePanel\|openProfile\|useProfilePanel" client/src/` returns nothing (panel fully removed)
3. `test ! -f client/src/context/ProfilePanelContext.jsx` passes (context deleted)
4. `test ! -f client/src/components/ProfilePanel.jsx` passes (component deleted)
5. `grep "to=\`/u/" client/src/components/PostCard.jsx client/src/components/PostListItem.jsx client/src/pages/ViewPost.jsx` shows Link navigation in all three files
6. `grep "artist.name" client/src/pages/Explore.jsx` shows correct field references
7. Vite dev server compiles without errors: `cd client && npx vite build --mode development 2>&1 | tail -5`
</verification>

<success_criteria>
- Explore page renders artists with correct names and images (field mismatch resolved)
- Clicking any author name on PostCard, PostListItem, or ViewPost navigates to /u/{username} (not a panel)
- No slide-out profile panel appears anywhere in the app
- ProfilePanel and ProfilePanelContext files are deleted
- App compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-browse-discovery-and-profiles/04-03-SUMMARY.md`
</output>
