---
phase: 19-scheduling-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/MyPosts.jsx
autonomous: true
requirements: [SCHED-07]

must_haves:
  truths:
    - "My Posts dashboard shows scheduled posts in a distinct 'Scheduled' section between Drafts and Published"
    - "Each scheduled post displays its scheduled date/time in the contributor's local timezone"
    - "Scheduled posts link to their edit page (like drafts do)"
  artifacts:
    - path: "client/src/pages/MyPosts.jsx"
      provides: "Scheduled section with date/time display"
      contains: "scheduled"
  key_links:
    - from: "client/src/pages/MyPosts.jsx"
      to: "/api/posts/mine"
      via: "posts.listMine() — already returns scheduledAt field from Phase 18"
      pattern: "scheduledAt"
---

<objective>
Add a "Scheduled" section to the My Posts dashboard showing scheduled posts with their scheduled date/time.

Purpose: Contributors need visibility into what posts are queued for future publication.
Output: MyPosts page with three sections: Drafts, Scheduled, Published.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-scheduling-backend/18-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs -->

From client/src/pages/MyPosts.jsx:
- Fetches posts via posts.listMine() → res.data.posts array
- Each post has: id, slug, body, status, createdAt, updatedAt, publishedAt, scheduledAt
- Currently filters into: drafts (status==='draft'), published (status==='published')
- Uses relativeTime() for date formatting
- Drafts link to /posts/:slug/edit, Published link to /posts/:slug

From server/routes/posts.js (Phase 18):
- GET /mine returns scheduledAt field for each post
- Scheduled posts have status='scheduled' and scheduledAt set to UTC ISO string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Scheduled section to My Posts dashboard</name>
  <files>client/src/pages/MyPosts.jsx</files>
  <action>
1. Add a third filter for scheduled posts alongside the existing drafts and published filters:
   ```
   const scheduled = allPosts.filter((p) => p.status === 'scheduled');
   ```

2. Add a "Scheduled" section between the Drafts and Published sections, following the exact same pattern as the Drafts section but with scheduling-specific styling:

   - Section header: "Scheduled" (same h2 style as "Drafts" and "Published")
   - Each scheduled post is a Link to `/posts/${post.slug}/edit` (same as drafts — clicking opens edit page where they can reschedule/cancel)
   - Display truncated body text (same truncate function)
   - Below the body, show metadata row with:
     - A blue "Scheduled" badge: `<span className="text-blue-600 dark:text-blue-400 font-medium">Scheduled</span>`
     - A middot separator
     - The scheduled date/time formatted in locale format: `new Date(post.scheduledAt).toLocaleString()` — this automatically displays in the browser's local timezone (satisfying SCHED-07's timezone requirement)
   - Same card styling as drafts/published (border, rounded-lg, hover transition)

3. Only render the Scheduled section if `scheduled.length > 0` (same guard as drafts/published).

4. Section order on page: Drafts → Scheduled → Published (most actionable first).

The key insight: `toLocaleString()` is sufficient for displaying scheduled times — it renders in the user's local timezone with a readable format like "3/20/2026, 2:30:00 PM". No library needed.
  </action>
  <verify>
    <automated>grep -n "scheduled\|scheduledAt\|toLocaleString\|Scheduled" client/src/pages/MyPosts.jsx</automated>
  </verify>
  <done>My Posts shows a "Scheduled" section with each post's scheduled date/time in local timezone, linking to the edit page. Section appears between Drafts and Published when scheduled posts exist.</done>
</task>

</tasks>

<verification>
1. Navigate to /my-posts with at least one scheduled post
2. "Scheduled" section appears between Drafts and Published
3. Each scheduled post shows a blue "Scheduled" badge and the scheduled date/time
4. Date/time displays in the browser's local timezone
5. Clicking a scheduled post navigates to its edit page
6. Section is hidden when no scheduled posts exist
</verification>

<success_criteria>
- MyPosts page has three sections: Drafts, Scheduled, Published
- Scheduled posts display their scheduled date/time in local timezone
- Scheduled posts link to edit page for reschedule/cancel actions
- Blue badge visually distinguishes scheduled posts from drafts
</success_criteria>

<output>
After completion, create `.planning/phases/19-scheduling-ui/19-02-SUMMARY.md`
</output>
