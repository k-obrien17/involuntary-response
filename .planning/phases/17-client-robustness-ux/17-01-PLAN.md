---
phase: 17-client-robustness-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/PostCard.jsx
  - client/src/components/PostListItem.jsx
  - client/src/pages/ArtistPage.jsx
  - client/src/pages/EditPost.jsx
  - client/src/components/EmbedInput.jsx
  - client/src/utils/formatDate.js
  - client/src/components/ProtectedRoute.jsx
  - client/src/components/EmbedPreview.jsx
  - client/src/pages/ViewPost.jsx
autonomous: true
requirements:
  - ROBU-01
  - ROBU-02
  - ROBU-03
  - ROBU-04
  - ROBU-05
  - ROBU-06
  - ROBU-07
  - ROBU-08
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Posts with missing tags or author data render without crashing"
    - "Publishing a draft on ViewPost re-fetches data instead of full page reload"
    - "Failed post deletion shows an error message to the user"
    - "EditPost does not flash unauthorized before auth context finishes loading"
    - "Embed iframes only contain allowlisted attributes"
    - "EmbedInput debounce timer is cleaned up on unmount"
    - "formatDate handles dates already ending with Z"
    - "ArtistPage does not double-decode URL params"
    - "ProtectedRoute dead code is removed"
  artifacts:
    - path: "client/src/components/PostCard.jsx"
      provides: "Null-safe post.tags and post.author access"
    - path: "client/src/components/PostListItem.jsx"
      provides: "Null-safe post.author access with fallbacks"
    - path: "client/src/pages/ArtistPage.jsx"
      provides: "No redundant decodeURIComponent"
    - path: "client/src/pages/EditPost.jsx"
      provides: "Auth loading gate + delete error message"
    - path: "client/src/components/EmbedInput.jsx"
      provides: "Debounce cleanup on unmount"
    - path: "client/src/utils/formatDate.js"
      provides: "Safe Z-suffix handling"
    - path: "client/src/components/EmbedPreview.jsx"
      provides: "Iframe attribute allowlist"
  key_links:
    - from: "client/src/pages/EditPost.jsx"
      to: "AuthContext.loading"
      via: "useAuth() destructuring"
      pattern: "const.*loading.*useAuth"
    - from: "client/src/pages/ViewPost.jsx"
      to: "posts.getBySlug"
      via: "re-fetch after publish"
      pattern: "posts\\.getBySlug"
---

<objective>
Fix all client-side robustness and UX issues identified in the v3.0 audit.

Purpose: Eliminate null-safety crashes, race conditions, dead code, resource leaks, and UX rough edges across 9 client files.
Output: Hardened client components that handle edge cases gracefully and provide proper user feedback.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@client/src/components/PostCard.jsx
@client/src/components/PostListItem.jsx
@client/src/pages/ArtistPage.jsx
@client/src/pages/EditPost.jsx
@client/src/components/EmbedInput.jsx
@client/src/utils/formatDate.js
@client/src/components/ProtectedRoute.jsx
@client/src/components/EmbedPreview.jsx
@client/src/pages/ViewPost.jsx
@client/src/context/AuthContext.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix client robustness issues (ROBU-01 through ROBU-08)</name>
  <files>
    client/src/components/PostCard.jsx
    client/src/components/PostListItem.jsx
    client/src/pages/ArtistPage.jsx
    client/src/pages/EditPost.jsx
    client/src/components/EmbedInput.jsx
    client/src/utils/formatDate.js
    client/src/components/ProtectedRoute.jsx
    client/src/components/EmbedPreview.jsx
  </files>
  <action>
    Apply 8 surgical fixes to separate files:

    **ROBU-01 — PostCard.jsx:** Add optional chaining for `post.tags` and `post.author`.
    - Line 36: `{post.tags.length > 0` → `{post.tags?.length > 0`
    - Line 52: `post.author.emailHash` → `post.author?.emailHash`
    - Line 53: `post.author.displayName` → `post.author?.displayName || 'Unknown'`
    - Line 57: `post.author.username` → `post.author?.username`
    - Line 60: `post.author.displayName` → `post.author?.displayName || 'Unknown'`

    **ROBU-02 — PostListItem.jsx:** Add optional chaining for `post.author` with fallback values.
    - Line 17-18: `post.author.emailHash` → `post.author?.emailHash`, `post.author.displayName` → `post.author?.displayName || 'Unknown'`
    - Line 22: `post.author.username` → `post.author?.username`
    - Line 26: `post.author.displayName` → `post.author?.displayName || 'Unknown'`

    **ROBU-03 — ArtistPage.jsx:** Remove the redundant `decodeURIComponent` call.
    - Line 8: Delete `const artistName = decodeURIComponent(name);` — React Router's `useParams()` already returns decoded values.
    - Replace all references to `artistName` with `name` throughout the file.

    **ROBU-04 — EditPost.jsx:** Wait for auth context `loading` to complete before checking ownership.
    - Add `loading` to the `useAuth()` destructure: `const { user, loading } = useAuth();`
    - In the `useEffect` fetch, add an early return if `loading` is true: `if (loading) return;` before the fetch call.
    - Add `loading` to the useEffect dependency array.
    - In the loading return (line 75), also return loading state if auth is still loading: `if (loading || loadingPost)` — rename the existing `loading` state to `loadingPost` to avoid name collision with auth `loading`.

    **ROBU-05 — EmbedInput.jsx:** Add cleanup of debounce timer on component unmount.
    - Add a `useEffect` cleanup that clears `debounceRef.current` on unmount:
      ```
      useEffect(() => {
        return () => {
          if (debounceRef.current) clearTimeout(debounceRef.current);
        };
      }, []);
      ```

    **ROBU-06 — formatDate.js:** The shared `utils/formatDate.js` file does NOT have the bug (it uses `new Date(dateString)` directly). The bug exists in two inline `formatDate` functions in `client/src/pages/admin/Invites.jsx` (line 14) and `client/src/pages/admin/Contributors.jsx` (line 13) which do `dateStr + 'Z'` without checking if it already ends with Z. Fix both:
    - Change `new Date(dateStr + 'Z')` to `new Date(dateStr.endsWith('Z') ? dateStr : dateStr + 'Z')` in both admin files.

    **ROBU-07 — ProtectedRoute.jsx:** Delete the file entirely. Confirm it is not imported anywhere (grep confirms only self-reference). Use `git rm` or delete the file.

    **ROBU-08 — EmbedPreview.jsx:** In the `dangerouslySetInnerHTML` path, after extracting the iframe tag with regex, parse and rebuild with only allowlisted attributes (src, width, height, allow, sandbox) instead of inserting the raw matched string.
    - Replace the `iframeOnly` usage with attribute extraction:
      ```
      const allowed = ['src', 'width', 'height', 'allow', 'sandbox'];
      const attrs = [...iframeOnly.matchAll(/(\w+)="([^"]*)"/g)]
        .filter(([, name]) => allowed.includes(name))
        .map(([, name, value]) => `${name}="${value}"`)
        .join(' ');
      const safeIframe = `<iframe ${attrs}></iframe>`;
      ```
    - Use `safeIframe` in the `dangerouslySetInnerHTML` instead of `iframeOnly`.
  </action>
  <verify>
    <automated>cd /Users/keithobrien/Desktop/Claude/Projects/involuntary-response && cd client && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - PostCard and PostListItem render without errors when post.tags or post.author is null/undefined
    - ArtistPage uses `name` from useParams directly (no decodeURIComponent)
    - EditPost waits for auth loading before checking ownership
    - EmbedInput cleans up debounce timer on unmount
    - Admin formatDate functions handle dates already ending with Z
    - ProtectedRoute.jsx is deleted (dead code removed)
    - EmbedPreview only passes allowlisted attributes to iframe
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix UX issues (UX-01, UX-02)</name>
  <files>
    client/src/pages/ViewPost.jsx
    client/src/pages/EditPost.jsx
  </files>
  <action>
    **UX-01 — ViewPost.jsx:** Replace `window.location.reload()` with a re-fetch of post data after publish.
    - In the publish button's onClick handler (around line 53-65), replace:
      ```
      navigate(`/posts/${post.slug}`);
      window.location.reload();
      ```
      with a re-fetch:
      ```
      const res = await posts.getBySlug(post.slug);
      setPost(res.data);
      setPublishing(false);
      ```
    - This updates the component state with the now-published post, which removes the draft banner and shows the published view without a full reload.

    **UX-02 — EditPost.jsx:** Show error message when post deletion fails.
    - In the `handleDelete` catch block (line 71-72), add `setError('Failed to delete post. Please try again.');` before `setDeleting(false);`.
    - The existing error display JSX (line 90-93) will render it automatically.
  </action>
  <verify>
    <automated>cd /Users/keithobrien/Desktop/Claude/Projects/involuntary-response && cd client && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Publishing a draft on ViewPost re-fetches and updates the UI without a full page reload
    - Failed post deletion on EditPost displays an error message to the user
  </done>
</task>

</tasks>

<verification>
1. `cd client && npx vite build` completes without errors
2. `grep -r "ProtectedRoute" client/src/` returns no results (dead code removed)
3. `grep -n "decodeURIComponent" client/src/pages/ArtistPage.jsx` returns no results
4. `grep -n "window.location.reload" client/src/pages/ViewPost.jsx` returns no results
5. `grep -n "post\.tags\.length" client/src/components/PostCard.jsx` returns no results (replaced with optional chaining)
</verification>

<success_criteria>
- All 10 requirement IDs (ROBU-01 through ROBU-08, UX-01, UX-02) addressed
- Client builds successfully with no errors
- No dead code (ProtectedRoute removed)
- No null-safety crashes on missing post.tags/post.author
- No UX regressions (publish flow, delete error handling)
</success_criteria>

<output>
After completion, create `.planning/phases/17-client-robustness-ux/17-01-SUMMARY.md`
</output>
