---
phase: 19-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/NotFound.jsx
  - client/src/App.jsx
  - client/src/pages/Search.jsx
  - client/src/pages/Explore.jsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Visiting /nonexistent-route renders a styled 404 page with a link to homepage"
    - "Visiting /foo/bar/baz renders the same 404 page (not a redirect to /)"
    - "Search page shows an error message with a retry button when the API call fails"
    - "Explore page shows an error message with a retry button when the API call fails"
    - "Clicking retry on Search re-fires the search query and clears the error on success"
    - "Clicking retry on Explore re-fetches explore data and clears the error on success"
  artifacts:
    - path: "client/src/pages/NotFound.jsx"
      provides: "404 page component"
      min_lines: 15
    - path: "client/src/App.jsx"
      provides: "Route config with NotFound catch-all"
      contains: "NotFound"
    - path: "client/src/pages/Search.jsx"
      provides: "Search with error state and retry"
      contains: "error"
    - path: "client/src/pages/Explore.jsx"
      provides: "Explore with error state and retry"
      contains: "error"
  key_links:
    - from: "client/src/App.jsx"
      to: "client/src/pages/NotFound.jsx"
      via: "Route path='*' element={<NotFound />}"
      pattern: 'path="\*".*NotFound'
    - from: "client/src/pages/Search.jsx"
      to: "../api/client"
      via: "retry button onClick re-calls searchApi.query"
      pattern: "setError.*null"
    - from: "client/src/pages/Explore.jsx"
      to: "../api/client"
      via: "retry button onClick re-calls browse.explore"
      pattern: "setError.*null"
---

<objective>
Add a styled 404 page for unknown routes and error-with-retry states to Search and Explore pages.

Purpose: Users currently see a silent redirect to home on unknown URLs and get no feedback when API calls fail on Search/Explore. This phase gives them clear, actionable feedback.
Output: NotFound.jsx page, updated App.jsx routing, updated Search.jsx and Explore.jsx with error states.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@client/src/App.jsx
@client/src/pages/Search.jsx
@client/src/pages/Explore.jsx
@client/src/pages/Home.jsx

<interfaces>
<!-- Design patterns from existing pages. Executor should match these exactly. -->

Layout wrapper pattern (from Home.jsx, Search.jsx, Explore.jsx):
```jsx
<main className="max-w-2xl mx-auto px-4 py-12">
  ...
</main>
```

Text color pattern (light/dark):
```jsx
// Headings
className="text-gray-900 dark:text-gray-100"
// Secondary/muted text
className="text-gray-400 dark:text-gray-500"
// Interactive text buttons
className="text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 text-sm transition"
```

API imports:
```jsx
// Search.jsx
import { search as searchApi } from '../api/client';
// Explore.jsx
import { browse } from '../api/client';
```

React Router Link:
```jsx
import { Link } from 'react-router-dom';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 404 page and update routing (UX-01)</name>
  <files>client/src/pages/NotFound.jsx, client/src/App.jsx</files>
  <action>
Create `client/src/pages/NotFound.jsx`:
- Default export `NotFound` component
- Uses the same layout wrapper as other pages: `<main className="max-w-2xl mx-auto px-4 py-12">`
- Centers content with `text-center`
- Shows "404" in large text (e.g., `text-6xl font-bold text-gray-900 dark:text-gray-100 mb-4`)
- Shows "Page not found" as a subtitle (`text-lg text-gray-400 dark:text-gray-500 mb-8`)
- Includes a `<Link to="/">` back to homepage, styled as a text link: `text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 transition`
- No animations, no illustrations -- text-only, matching the minimal design of the rest of the app

Update `client/src/App.jsx`:
- Add import: `import NotFound from './pages/NotFound';`
- Replace the catch-all route `<Route path="*" element={<Navigate to="/" replace />} />` with `<Route path="*" element={<NotFound />} />`
- Remove the `Navigate` import from react-router-dom (it is no longer used anywhere in the file)
  </action>
  <verify>
    <automated>cd /Users/keithobrien/client && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Unknown routes render a styled 404 page with "404", "Page not found" text, and a link to homepage. The old redirect-to-home behavior is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add error states with retry to Search and Explore (UX-02)</name>
  <files>client/src/pages/Search.jsx, client/src/pages/Explore.jsx</files>
  <action>
Update `client/src/pages/Search.jsx`:
- Add `const [error, setError] = useState(null);` alongside existing state
- In the `fetchResults` function inside useEffect: set `setError(null)` before the try block, and `setError('Something went wrong. Please try again.')` in the catch block (keep the console.error too)
- In the `loadMore` function: set `setError(null)` before the try block, and `setError('Failed to load more results.')` in the catch block
- Add an error display block: after the `if (loading)` early return and before the main return, add `if (error && posts.length === 0)` early return that shows:
  - Same layout wrapper `<main className="max-w-2xl mx-auto px-4 py-12">`
  - `<div className="text-center">` containing:
  - Error message in `<p className="text-gray-400 dark:text-gray-500 mb-4">{error}</p>`
  - Retry button: `<button onClick={() => { setError(null); /* re-trigger by forcing effect */ const params = new URLSearchParams(window.location.search); window.location.search = params.toString(); }} className="text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 text-sm transition">Retry</button>`
  - Actually, a cleaner retry approach: extract fetchResults out of the useEffect into a standalone function declared at the component level (still using `q`). Call it from useEffect and from the retry button's onClick. The retry button calls `fetchResults()` directly. This avoids a page reload.

Update `client/src/pages/Explore.jsx`:
- Add `const [error, setError] = useState(null);` alongside existing state
- In the `fetchExplore` function inside useEffect: set `setError(null)` before the try block, and `setError('Something went wrong. Please try again.')` in the catch block
- Replace the existing `if (!data)` block (which shows "Failed to load explore data." with no retry) with an error-aware block: `if (error || !data)` that shows:
  - Same layout wrapper `<main className="max-w-2xl mx-auto px-4 py-8">` (note: Explore uses py-8, not py-12)
  - `<div className="text-center">` containing:
  - Error message: `<p className="text-gray-400 dark:text-gray-500 mb-4">{error || 'Failed to load explore data.'}</p>`
  - Retry button: `<button onClick={retryFn} className="text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 text-sm transition">Retry</button>`
  - Extract `fetchExplore` out of useEffect into a standalone function. Call it from useEffect and from the retry button's onClick.

Retry button pattern for both pages:
```jsx
<button
  onClick={fetchFn}
  className="text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 text-sm transition"
>
  Retry
</button>
```

Important: both retry functions must reset `setLoading(true)` and `setError(null)` at the start so the loading state shows while retrying.
  </action>
  <verify>
    <automated>cd /Users/keithobrien/client && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Search page displays error message with retry button when API call fails (instead of silent console.error). Explore page displays error message with retry button when API call fails (replacing the static "Failed to load" text with no retry). Clicking retry re-fires the API call.</done>
</task>

</tasks>

<verification>
1. `cd /Users/keithobrien/client && npx vite build` completes without errors
2. Grep NotFound.jsx for Link to="/" (homepage link present)
3. Grep App.jsx for `path="*"` -- should reference NotFound, not Navigate
4. Grep Search.jsx for "error" state and "Retry" button text
5. Grep Explore.jsx for "error" state and "Retry" button text
6. Confirm Navigate import removed from App.jsx (no longer needed)
</verification>

<success_criteria>
- Unknown routes (e.g., /asdfgh, /foo/bar) render a 404 page with "Page not found" and a link home
- Search page shows "Something went wrong" + Retry button on API failure
- Explore page shows "Something went wrong" + Retry button on API failure
- Clicking Retry re-fires the data fetch
- Vite build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-ux-polish/19-01-SUMMARY.md`
</output>
