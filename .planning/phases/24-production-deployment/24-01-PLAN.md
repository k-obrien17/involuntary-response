---
phase: 24-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/vercel.json
  - client/public/robots.txt
  - client/public/sitemap.xml
autonomous: false
requirements:
  - LNCH-07
  - LNCH-08
must_haves:
  truths:
    - "Vercel frontend loads the site with no build errors"
    - "API requests from the frontend reach the Render backend"
    - "Backend health endpoint responds on Render"
  artifacts:
    - path: "client/vercel.json"
      provides: "API proxy rewrite to real Render URL"
      contains: "onrender.com/api"
    - path: "client/public/robots.txt"
      provides: "Sitemap URL with real domain"
    - path: "client/public/sitemap.xml"
      provides: "Site URLs with real domain"
  key_links:
    - from: "client/vercel.json"
      to: "Render backend"
      via: "Vercel rewrite rule for /api/*"
      pattern: "https://.*\\.onrender\\.com/api/"
---

<objective>
Deploy the latest code (v3.0 through v4.1) to production. Replace placeholder URLs in config files with real deployment URLs, push to trigger Vercel deploy, and verify the full stack is live.

Purpose: This is the final phase -- get everything live so people can use the site.
Output: Working production deployment on Vercel + Render + Turso.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@client/vercel.json
@client/public/robots.txt
@client/public/sitemap.xml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Collect deployment URLs from user</name>
  <files>client/vercel.json, client/public/robots.txt, client/public/sitemap.xml</files>
  <action>
    Ask the user for two values needed to replace placeholders:
    1. Render backend hostname (e.g., involuntary-response-api.onrender.com) -- used in vercel.json API proxy
    2. Frontend domain (e.g., involuntary-response.vercel.app or custom domain) -- used in robots.txt and sitemap.xml

    Also confirm that the following Render env vars are configured:
    ADMIN_EMAIL, ADMIN_PASSWORD, ADMIN_DISPLAY_NAME, TURSO_DATABASE_URL, TURSO_AUTH_TOKEN, JWT_SECRET, FRONTEND_URL
  </action>
  <verify>User has provided both URLs and confirmed env vars are set</verify>
  <done>Render hostname and frontend domain are known; Render env vars confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Update placeholder URLs in config files</name>
  <files>client/vercel.json, client/public/robots.txt, client/public/sitemap.xml</files>
  <action>
    Replace all placeholder URLs with the real values provided in Task 1:

    1. In `client/vercel.json`: Replace `YOUR-APP.onrender.com` with the actual Render hostname on line 15
    2. In `client/public/robots.txt`: Replace `YOURDOMAIN.com` with the actual frontend domain on line 7
    3. In `client/public/sitemap.xml`: Replace all 4 occurrences of `YOURDOMAIN.com` with the actual frontend domain (lines 4, 9, 14, 19)

    Do NOT change any other content in these files. The rewrite rules, robot directives, and sitemap structure stay exactly as they are.
  </action>
  <verify>
    <automated>grep -c "YOUR-APP\|YOURDOMAIN" client/vercel.json client/public/robots.txt client/public/sitemap.xml | grep -v ":0$" && echo "FAIL: placeholders remain" || echo "PASS: all placeholders replaced"</automated>
  </verify>
  <done>All three files contain real URLs with zero placeholder strings remaining</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify production deployment</name>
  <files>client/vercel.json</files>
  <action>
    After pushing the updated config files to GitHub (which triggers Vercel redeploy), verify the full production stack is working. Render should already be running the latest server code from main branch.

    Run automated checks first:
    - curl the Render health endpoint to confirm backend is live
    - curl an API endpoint through Vercel proxy to confirm routing works

    Then ask the user to verify the frontend visually.
  </action>
  <verify>curl to health endpoint returns 200; frontend loads in browser</verify>
  <done>Frontend loads, API proxy works, backend responds -- full stack is live</done>
</task>

</tasks>

<verification>
- Zero placeholder strings remain in vercel.json, robots.txt, sitemap.xml
- Vercel build succeeds (check Vercel dashboard or `vercel ls`)
- Frontend loads at production URL
- API proxy routes forward to Render backend (no 502s)
- Backend health check returns 200
</verification>

<success_criteria>
1. Vercel frontend build succeeds with no errors and the deployed site loads correctly
2. API proxy routes on Vercel correctly forward requests to the Render backend
3. Backend on Render is running the latest server code (scheduling, analytics endpoints respond)
</success_criteria>

<output>
After completion, create `.planning/phases/24-production-deployment/24-01-SUMMARY.md`
</output>
