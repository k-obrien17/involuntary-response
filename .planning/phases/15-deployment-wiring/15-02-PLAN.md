---
phase: 15-deployment-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/vercel.json
  - client/public/robots.txt
  - client/public/sitemap.xml
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-04

must_haves:
  truths:
    - "Vercel API proxy routes resolve to the actual Render backend (not a placeholder string)"
    - "Visiting /robots.txt returns a valid robots file"
    - "Visiting /sitemap.xml returns a valid sitemap listing public routes"
  artifacts:
    - path: "client/vercel.json"
      provides: "API proxy rewrites with real Render backend URL"
      contains: "onrender.com"
    - path: "client/public/robots.txt"
      provides: "Robots file allowing crawlers with sitemap reference"
      contains: "Sitemap"
    - path: "client/public/sitemap.xml"
      provides: "XML sitemap listing public routes"
      contains: "urlset"
  key_links:
    - from: "client/vercel.json"
      to: "Render backend"
      via: "Hardcoded URL in rewrite destination (env vars not supported in Vercel rewrites)"
      pattern: "onrender\\.com"
---

<objective>
Replace the Vercel API proxy placeholder with the real Render backend URL, and add robots.txt and sitemap.xml for search engine discovery.

Purpose: The Vercel deployment cannot proxy API requests because vercel.json contains a literal placeholder string instead of a real URL. Robots/sitemap files are needed for SEO.
Output: Working vercel.json proxy config, robots.txt, and sitemap.xml in client/public/.
</objective>

<execution_context>
@/Users/keithobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keithobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From client/vercel.json (current -- has placeholder):
```json
{
  "rewrites": [
    { "source": "/posts/:slug", "destination": "/api/og" },
    { "source": "/api/:path*", "destination": "https://RENDER_BACKEND_URL/api/:path*" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
```

Public routes from App.jsx (for sitemap):
- / (Home)
- /explore (Explore page)
- /search (Search page)
- /join (Reader registration)
- /posts/:slug (dynamic -- skip in static sitemap)
- /u/:username (dynamic -- skip in static sitemap)
- /tag/:tag (dynamic -- skip in static sitemap)
- /artist/:name (dynamic -- skip in static sitemap)

Key constraint: Vercel rewrites do NOT support environment variable interpolation in the destination field (confirmed in Phase 6 summary). The backend URL must be hardcoded in vercel.json.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Get Render backend URL and production domain from user</name>
  <files>client/vercel.json</files>
  <action>
  Ask the user for two pieces of information needed before proceeding:
  1. Their Render backend URL (e.g., https://involuntary-response-api.onrender.com)
  2. Their production domain (e.g., involuntaryresponse.com or their-app.vercel.app)

  Vercel rewrites do not support env var interpolation in destination fields, so the Render URL must be hardcoded in vercel.json. The production domain is needed for robots.txt Sitemap directive and sitemap.xml URLs.
  </action>
  <verify>User has provided both URLs</verify>
  <done>Render backend URL and production domain captured for use in Task 2</done>
</task>

<task type="auto">
  <name>Task 2: Update vercel.json and create robots.txt + sitemap.xml</name>
  <files>client/vercel.json, client/public/robots.txt, client/public/sitemap.xml</files>
  <action>
  **client/vercel.json**: Replace `https://RENDER_BACKEND_URL` with the actual Render backend URL provided by the user. Keep all three rewrite rules in the same order:
  1. `/posts/:slug` -> `/api/og` (OG meta tag handler)
  2. `/api/:path*` -> `https://{ACTUAL_RENDER_URL}/api/:path*` (API proxy)
  3. `/(.*)`-> `/` (SPA fallback)

  **client/public/robots.txt**: Create with:
  ```
  User-agent: *
  Allow: /
  Disallow: /admin
  Disallow: /admin/
  Disallow: /api/

  Sitemap: https://{PRODUCTION_DOMAIN}/sitemap.xml
  ```
  Use the production domain provided by the user.

  **client/public/sitemap.xml**: Create a valid XML sitemap listing static public routes only (dynamic routes like /posts/:slug need a server-generated sitemap, which is Phase 16+ territory). Include:
  - `/` (priority 1.0, changefreq weekly)
  - `/explore` (priority 0.8, changefreq weekly)
  - `/search` (priority 0.6, changefreq monthly)
  - `/join` (priority 0.5, changefreq monthly)

  Do NOT include: auth pages (login, register, forgot-password), admin pages, or dynamic routes.
  Use `https://{PRODUCTION_DOMAIN}` as the base URL. Set `<lastmod>` to 2026-03-01.
  Format with `xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"` and XML declaration.
  </action>
  <verify>
  ```
  # Verify vercel.json has no placeholder
  cd /Users/keithobrien && node -e "const v = JSON.parse(require('fs').readFileSync('client/vercel.json','utf8')); const dest = v.rewrites[1].destination; if (dest.includes('RENDER_BACKEND_URL')) { console.error('FAIL: still has placeholder'); process.exit(1); } console.log('vercel.json OK:', dest);"

  # Verify robots.txt
  grep "Sitemap:" client/public/robots.txt

  # Verify sitemap.xml has correct number of URLs
  grep -c "<url>" client/public/sitemap.xml
  ```
  Expected: vercel.json shows real URL, robots.txt has Sitemap directive, sitemap.xml has 4 URL entries.
  </verify>
  <done>vercel.json proxies API requests to the actual Render backend URL. robots.txt exists with crawl directives. sitemap.xml lists 4 public routes in valid XML format.</done>
</task>

</tasks>

<verification>
1. `cat client/vercel.json` -- second rewrite destination is a real .onrender.com URL
2. `cat client/public/robots.txt` -- has User-agent, Allow, Disallow, Sitemap directives
3. `cat client/public/sitemap.xml` -- valid XML with 4 url entries
4. No `RENDER_BACKEND_URL` literal remains in vercel.json
</verification>

<success_criteria>
- vercel.json API rewrite resolves to a real Render backend URL (not placeholder text)
- robots.txt serves proper crawl directives including Sitemap reference
- sitemap.xml lists public routes (/, /explore, /search, /join) in valid XML sitemap format
- All files in client/public/ will be served at root paths by Vercel
</success_criteria>

<output>
After completion, create `.planning/phases/15-deployment-wiring/15-02-SUMMARY.md`
</output>
